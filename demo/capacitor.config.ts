import type { CapacitorConfig } from "@capacitor/cli";
import { capacitor } from "./appspresso.config";

/**
 * Capacitor CLI entry — same object as `capacitor.config.json`.
 * Edit `appspresso.config.ts` only; run `npm run demo:cap-config` to refresh JSON for sync.
 */
export default capacitor satisfies CapacitorConfig;
