# Plugin host example

Demonstrates `AppspressoHost` with `@appspresso/plugin-sentry`.

```bash
npm install
cp .env.example .env
npm run dev
npm run doctor
```

Set `VITE_SENTRY_DSN` in `.env` to enable Sentry (requires `@sentry/react` in your app).
